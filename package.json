{
  "name": "linkly",
  "version": "0.1.0",
  "private": true,
  "description": "Linkly monorepo — web app, shared schemas, CLI, browser extension.",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @linkly/web dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @linkly/web start",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:unit": "pnpm -r test:unit",
    "test:integration": "pnpm --filter @linkly/web test:integration",
    "test:e2e": "pnpm --filter @linkly/web test:e2e",
    "test:e2e:install": "pnpm --filter @linkly/web test:e2e:install",
    "worker": "pnpm --filter @linkly/web worker",
    "worker:dev": "pnpm --filter @linkly/web worker:dev",
    "webhook-worker": "pnpm --filter @linkly/web webhook-worker",
    "webhook-worker:dev": "pnpm --filter @linkly/web webhook-worker:dev",
    "db:migrate": "pnpm --filter @linkly/web db:migrate",
    "db:migrate:undo": "pnpm --filter @linkly/web db:migrate:undo",
    "db:migrate:undo:all": "pnpm --filter @linkly/web db:migrate:undo:all"
  },
  "devDependencies": {
    "typescript": "^5"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=20"
  }
}
