{
  "name": "@cortala/schemas",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod schemas for Cortala's public REST API. Single source of truth for the web app, CLI and browser extension.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./v1": "./src/v1.ts",
    "./webhooks": "./src/webhooks.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo '(no lint configured for schemas)'",
    "test": "echo '(no tests in schemas package — covered by web/tests/unit/schemas)'"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
